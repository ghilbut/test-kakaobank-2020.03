[
  {
    "name": "${name}",
    "image": "${image}",
    "essential": true,
    "portMappings": [
      {
        "protocol": "tcp",
        "containerPort": ${port}
      }
    ],
    "environment": ${environment},
    "secrets": ${secrets},
    "logConfiguration": ${log_config}
  }
]
